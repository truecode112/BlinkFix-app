import React from 'react';
import {StyleSheet} from 'react-native';

export const SingleOrderInProfileStyles = StyleSheet.create({
  container: {
    paddingVertical: 20,
    paddingHorizontal: 5,
    position: 'relative',
    width: '100%',
  },
  titleContainer: {
    flexDirection: 'row',
  },
});
