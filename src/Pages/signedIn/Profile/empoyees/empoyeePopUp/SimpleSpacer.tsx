import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const SimpleSpacer = ({height}: {height: number}) => {
  return <View style={{height}}></View>;
};

export default SimpleSpacer;

const styles = StyleSheet.create({});
